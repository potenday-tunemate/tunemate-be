<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tunemate.be.domain.user.domain.user.UserMapper">
    <resultMap id="UserResultMap" type="com.tunemate.be.domain.user.domain.user.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
    </resultMap>
    <select id="findByEmail" resultType="com.tunemate.be.domain.user.domain.user.User">
        SELECT *
        FROM user
        WHERE email = #{email}
    </select>
    <insert id="create" parameterType="com.tunemate.be.domain.user.domain.user.CreateUserDTO">
        INSERT INTO user (email, password, nickname)
        VALUES (#{email}, #{password}, #{nickname})
    </insert>
</mapper>